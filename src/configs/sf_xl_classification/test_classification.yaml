# Smoke-test config: minimal steps, capped val/test batches, small batch size.
logger:
  - type: wandb
    project: placeforge_classification
    name: test_classification
  - type: csv
    name: csv_logs

trainer:
  max_steps: 5
  max_epochs: -1
  accelerator: gpu
  devices: 1
  precision: 16-mixed
  log_every_n_steps: 1
  val_check_interval: 5
  check_val_every_n_epoch: null
  gradient_clip_val: 1.0
  limit_val_batches: 2
  limit_test_batches: 2

compile: false

module:
  name: classification
  model_name: dinov2_boq
  learning_rate: 1.0e-4
  weight_decay: 1.0e-4
  warmup_steps: 2
  backbone_lr_scale: 0.1
  criterion_lr_scale: 10.0
  lr_schedule: constant
  val_recall_ks: [1, 5, 10]

datamodule:
  name: place_recognition
  train_dataset_name: sf_xl_cosplace
  dataset_type: classification
  sampler:
    num_places: 100
  val_dataset_names:
    - pitts30k_val
  test_dataset_names:
    - pitts30k_test
  batch_size: 4
  images_per_place: 4
  num_workers: 2
  transform:
    name: train
    image_size: 224
  val_transform:
    name: eval
    image_size: 224
  test_transform:
    name: eval
    image_size: 224
