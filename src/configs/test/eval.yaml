# Model/eval spec for `python -m cli test src/configs/test/eval.yaml`.
# Keep model details here; put dataset and dataloader settings in `data_config`.
# Supports .ckpt, .pth, .pt files. Standalone weight files use robust loading.
# Available architectures:
# dinov2_gem | dinov2_salad | dinov2_boq | resnet18_gem
# selavpr_base_boq | selavpr_large_boq | selavpr_base_gem | selavpr_large_gem
# selavpr_base_salad | selavpr_large_salad
# megaloc | boq | salad | eigenplaces | mixvpr | supervlad | sage
checkpoint: /home/oliver/github/placeforge/checkpoints/train/sf_xl_salad/best.ckpt
data_config: test_config.yaml
architecture: dinov2_salad
image_size: 224

trainer:
  accelerator: gpu
  devices: 1
  precision: 16-mixed
